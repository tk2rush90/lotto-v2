import { InfoContentDirective } from './info-content.directive';

describe('InfoContentDirective', () => {
  it('should create an instance', () => {
    const directive = new InfoContentDirective();
    expect(directive).toBeTruthy();
  });
});
